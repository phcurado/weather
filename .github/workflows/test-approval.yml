name: test-approval

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  needs_approval:
    runs-on: ubuntu-latest
    environment: test-approval
    steps:
      - run: echo "Approved — runner started"
