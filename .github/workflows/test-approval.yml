name: test-approval

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  needs_approval:
    runs-on: ubuntu-latest
    environment: test-approval
    steps:
      - run: echo "Approved — runner started"
